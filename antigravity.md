# Antigravity Agent Configuration

The Antigravity agent inherits all ingestion schemas, skills, page formatting, and runtime rules from [`claude.md`](claude.md).

Agent-specific override: use `engine_used: Antigravity` when this agent executes the digest. No other configuration is duplicated here.
