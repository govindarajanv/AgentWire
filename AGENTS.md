# Agent Instructions

## Commit and Push After Changes

After making changes to files in this repository and successful testing/validation, always commit and push the changes. Do not leave working directory changes uncommitted unless explicitly asked to wait.

Sequence:
1. Run relevant tests or validation commands.
2. `git add` the changed files.
3. `git commit` with a concise message matching the repo style.
4. `git push`.
